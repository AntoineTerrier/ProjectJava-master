package contract;

public enum ControllerOrder
{
	/** Action : go right. */
	RIGHT,
	/** Action : go left. */
	LEFT,
	/** Action : go up. */
	UP,
	/** Action : go down. */
	DOWN,
	/** No action. */
	STILL
}
